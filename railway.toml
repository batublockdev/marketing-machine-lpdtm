[build]
builder = "DOCKERFILE"

[deploy]
preDeployCommand = "node node_modules/prisma/build/index.js db push"
startCommand = "node server.js"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3